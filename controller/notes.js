const {Note} = require('../models/note')

const notes = (req,res)=>{
    try {
        const body = req
        
    } catch (error) {
        console.log(error);
        
    }


}
module.exports = {
    notes,
}